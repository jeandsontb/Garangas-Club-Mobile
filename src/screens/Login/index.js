import React from 'react';

import Styled from './style';

export default () => {
    return (
        <Styled.Container>
            <Styled.Text>Tela de login</Styled.Text>
        </Styled.Container>
    );
};
